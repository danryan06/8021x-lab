#!/usr/bin/env bash
# Convenience wrapper — prefers the API CA adapter once the stack is up.
set -euo pipefail

echo "Prefer: POST /api/ca/ensure-root and /api/ca/issue-client after login."
echo "This script is a placeholder for offline openssl helpers in later phases."
exit 0
